import pandas as pd
import traceback

inegi_excel = r"c:\Users\aicil\OneDrive\Escritorio\PVU\SARAMPIÓN\mezquital\POBLACION\POBLACION_MEZQUITAL_INEGI.xlsx"

try:
    xl = pd.ExcelFile(inegi_excel)
    print("Sheets:", xl.sheet_names)
    
    if 'Hoja1' in xl.sheet_names:
        df_inegi = pd.read_excel(inegi_excel, sheet_name='Hoja1', header=None)
        print("\n--- Hoja1 ---")
        for i in range(15):
            print(f"Row {i}:", [str(x) for x in df_inegi.iloc[i].tolist() if str(x) != 'nan'])

except Exception as e:
    traceback.print_exc()
